version: '3.8'

services:
 app:
  image: mysql:latest
  container_name: mysql-komunikasi
  ports:
   - "3306:3306"
  environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_DATABASE: db_komunikasi
  volumes:
   - db_data:/var/lib/mysql

volumes:
  db_data: